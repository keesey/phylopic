{
    "dependencies": {
        "@aws-sdk/client-lambda": "^3.52.0",
        "@aws-sdk/client-s3": "^3.52.0",
        "@aws-sdk/client-ssm": "^3.52.0",
        "@phylopic/api-models": "*",
        "@phylopic/source-client": "*",
        "@phylopic/source-models": "*",
        "@phylopic/utils": "*",
        "@phylopic/utils-aws": "*",
        "axios": "^0.27.2",
        "concurrently": "^7.0.0",
        "dotenv": "^16.0.0",
        "metadata-scraper": "^0.2.49",
        "parse-nomen": "^2.1.2",
        "path": "^0.12.7",
        "pg": "^8.7.3",
        "probe-image-size": "^7.2.3",
        "readline": "^1.3.0",
        "simple-digraph": "^1.0.1",
        "ts-node": "^10.8.0"
    },
    "devDependencies": {
        "@phylopic/tsconfig": "*",
        "@types/probe-image-size": "^7.0.1",
        "eslint-config-phylopic": "*",
        "typescript": "^4.6.4"
    },
    "name": "@phylopic/publish",
    "private": true,
    "scripts": {
        "clean": "rm -rf .turbo && rm -rf node_modules",
        "cli": "ts-node --esm ./src/cli.ts",
        "download": "concurrently \"yarn download:images\" \"yarn download:upload\"",
        "download:images": "aws s3 sync --delete s3://images.phylopic.org/ ./.s3/images.phylopic.org && find .s3/images.phylopic.org -type d -empty -delete",
        "download:upload": "aws s3 sync --delete s3://upload.phylopic.org/ ./.s3/upload.phylopic.org && find .s3/upload.phylopic.org -type d -empty -delete",
        "heal": "ts-node --esm ./src/heal.ts",
        "insert": "ts-node --esm ./src/insert.ts 2> insert-error.log",
        "lint": "TIMING=1 eslint --ext .js,.ts . --fix",
        "make": "yarn download && yarn process && concurrently \"yarn insert\" \"yarn upload\" && yarn release && yarn sync",
        "process": "./process.sh 2> process-error.log",
        "release": "ts-node --esm ./src/release.ts 2> release-error.log",
        "sync": "yarn sync:images",
        "sync:images": "aws s3 sync --acl public-read --delete ./.s3/images.phylopic.org s3://images.phylopic.org/",
        "upload": "yarn upload:images",
        "upload:images": "aws s3 sync --acl public-read ./.s3/images.phylopic.org s3://images.phylopic.org/"
    },
    "type": "module",
    "version": "0.0.0"
}
