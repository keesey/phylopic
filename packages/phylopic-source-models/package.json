{
    "author": "T. Michael Keesey <keesey@gmail.com>",
    "dependencies": {
        "email-validator": "^2.0.4",
        "parse-nomen": "^2.1.2",
        "phylopic-utils": "file:../phylopic-utils",
        "uuid": "^8.3.2"
    },
    "description": "Types, validation, and normalization for PhyloPic model objects.",
    "files": [
        "dist/**/*"
    ],
    "license": "MIT",
    "main": "dist/index.js",
    "name": "phylopic-source-models",
    "private": false,
    "resolutions": {
        "@types/node": "*"
    },
    "scripts": {
        "build": "rm -rf dist && mkdir dist && yarn compile",
        "compile": "tsc",
        "format": "prettier -w . && yarn lint --fix",
        "lint": "eslint --ext .js,.ts .",
        "postversion": "git push && git push --tags",
        "prepare": "yarn build",
        "prepublishOnly": "yarn test && yarn lint",
        "preversion": "yarn format",
        "test": "mocha -r ts-node/register \"./src/**/*.spec.ts\"",
        "version": "git add -A src"
    },
    "type": "module",
    "types": "dist/index.d.ts",
    "version": "2.0.0-alpha.1"
}
